[project]
name = "downloader-bot"
version = "1.0.0"
description = "Telegram bot for downloading videos"
requires-python = ">=3.11"
dependencies = [
    "aiohttp>=3.9",
    "aiogram>=3.0",
    "python-dotenv>=1.2.2",
    "yt-dlp",
    "curl-cffi",
]
